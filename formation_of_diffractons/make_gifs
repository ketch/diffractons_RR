#!/bin/bash

clear
mkdir animations
echo "*****************************"
echo "*** Script to create GIFs ***"
echo "*****************************"
echo " "

echo "sigma"
echo "  pcolor"
path="./_plots_animation/_p/pcolor"
convert -delay 5 -loop 0 "$path/*.png" -delay 200 "$path/*0400*" "./animations/sigma_pcolor.gif"
echo "  surface"
path="./_plots_animation/_p/surface"
convert -delay 5 -loop 0 "$path/*.png" -delay 200 "$path/*0400*" "./animations/sigma_surface.gif"
echo "  slices"
path="./_plots_animation/_p/slices"
convert -delay 5 -loop 0 "$path/*.eps" -delay 200 "$path/*0400*" "./animations/sigma_slices.gif"

echo "q0"
echo "  pcolor"
path="./_plots_animation/_q0/pcolor"
convert -delay 5 -loop 0 "$path/*.png" -delay 200 "$path/*0400*" "./animations/q0_pcolor.gif"
echo "  surface"
path="./_plots_animation/_q0/surface"
convert -delay 5 -loop 0 "$path/*.png" -delay 200 "$path/*0400*" "./animations/q0_surface.gif"
echo "  slices"
path="./_plots_animation/_q0/slices"
convert -delay 5 -loop 0 "$path/*.eps" -delay 200 "$path/*0400*" "./animations/q0_slices.gif"

echo "q2"
echo "  pcolor"
path="./_plots_animation/_q2/pcolor"
convert -delay 5 -loop 0 "$path/*.png" -delay 200 "$path/*0400*" "./animations/q2_pcolor.gif"
echo "  surface"
path="./_plots_animation/_q2/surface"
convert -delay 5 -loop 0 "$path/*.png" -delay 200 "$path/*0400*" "./animations/q2_surface.gif"
echo "  slices"
path="./_plots_animation/_q2/slices"
convert -delay 5 -loop 0 "$path/*.eps" -delay 200 "$path/*0400*" "./animations/q2_slices.gif"
